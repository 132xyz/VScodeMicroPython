    1: from __future__ import annotations
       
    1: from typing import TYPE_CHECKING, Iterable, List, TypeVar, cast, overload
       
    1: from prompt_toolkit.formatted_text.base import OneStyleAndTextTuple
       
    1: if TYPE_CHECKING:
           from typing_extensions import SupportsIndex
       
    1: __all__ = [
    1:     "explode_text_fragments",
       ]
       
    1: _T = TypeVar("_T", bound=OneStyleAndTextTuple)
       
       
    2: class _ExplodedList(List[_T]):
    1:     """
           Wrapper around a list, that marks it as 'exploded'.
       
           As soon as items are added or the list is extended, the new items are
           automatically exploded as well.
           """
       
    1:     exploded = True
       
    1:     def append(self, item: _T) -> None:
               self.extend([item])
       
    1:     def extend(self, lst: Iterable[_T]) -> None:
               super().extend(explode_text_fragments(lst))
       
    1:     def insert(self, index: SupportsIndex, item: _T) -> None:
               raise NotImplementedError  # TODO
       
           # TODO: When creating a copy() or [:], return also an _ExplodedList.
       
    2:     @overload
    2:     def __setitem__(self, index: SupportsIndex, value: _T) -> None: ...
       
    2:     @overload
    2:     def __setitem__(self, index: slice, value: Iterable[_T]) -> None: ...
       
    1:     def __setitem__(
               self, index: SupportsIndex | slice, value: _T | Iterable[_T]
           ) -> None:
               """
               Ensure that when `(style_str, 'long string')` is set, the string will be
               exploded.
               """
               if not isinstance(index, slice):
                   int_index = index.__index__()
                   index = slice(int_index, int_index + 1)
               if isinstance(value, tuple):  # In case of `OneStyleAndTextTuple`.
                   value = cast("List[_T]", [value])
       
               super().__setitem__(index, explode_text_fragments(value))
       
       
    1: def explode_text_fragments(fragments: Iterable[_T]) -> _ExplodedList[_T]:
           """
           Turn a list of (style_str, text) tuples into another list where each string is
           exactly one character.
       
           It should be fine to call this function several times. Calling this on a
           list that is already exploded, is a null operation.
       
           :param fragments: List of (style, text) tuples.
           """
           # When the fragments is already exploded, don't explode again.
   61:     if isinstance(fragments, _ExplodedList):
               return fragments
       
   61:     result: list[_T] = []
       
  122:     for style, string, *rest in fragments:
  305:         for c in string:
  244:             result.append((style, c, *rest))  # type: ignore
       
   61:     return _ExplodedList(result)
