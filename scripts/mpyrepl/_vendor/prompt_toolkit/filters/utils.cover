    1: from __future__ import annotations
       
    1: from .base import Always, Filter, FilterOrBool, Never
       
    1: __all__ = [
    1:     "to_filter",
    1:     "is_true",
       ]
       
       
    1: _always = Always()
    1: _never = Never()
       
       
    1: _bool_to_filter: dict[bool, Filter] = {
    1:     True: _always,
    1:     False: _never,
       }
       
       
    1: def to_filter(bool_or_filter: FilterOrBool) -> Filter:
           """
           Accept both booleans and Filters as input and
           turn it into a Filter.
           """
16323:     if isinstance(bool_or_filter, bool):
 5740:         return _bool_to_filter[bool_or_filter]
       
10583:     if isinstance(bool_or_filter, Filter):
10583:         return bool_or_filter
       
           raise TypeError(f"Expecting a bool or a Filter instance. Got {bool_or_filter!r}")
       
       
    1: def is_true(value: FilterOrBool) -> bool:
           """
           Test whether `value` is True. In case of a Filter, call it.
       
           :param value: Boolean or `Filter` instance.
           """
  166:     return to_filter(value)()
